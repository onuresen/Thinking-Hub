{
  "name": "thinking-hub-tests",
  "private": true,
  "description": "Dev-only smoke tests for Thinking Hub. The app itself has no build step and no runtime dependencies — this folder is tooling only.",
  "scripts": {
    "test": "node smoke.js"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
